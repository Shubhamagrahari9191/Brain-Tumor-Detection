{
  "name": "brain-tumor-detection-root",
  "version": "1.0.0",
  "description": "Root workspace configuration to run frontend and backend together",
  "scripts": {
    "install-all": "npm install --prefix frontend && npm install --prefix backend",
    "build": "npm run install-all && npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "dev": "npm start --prefix backend & npm run dev --prefix frontend"
  }
}
